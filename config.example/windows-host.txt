# Windows host apps (winget ids), one per line. '#' = comment.
# Prefix with "choco:" to use the Chocolatey fallback, e.g. choco:some-package
Microsoft.WindowsTerminal
AgileBits.1Password
SlackTechnologies.Slack
Mozilla.Firefox
Microsoft.VisualStudioCode
Anthropic.Claude
Obsidian.Obsidian
# Syncthing for ~/Sync folder sync (WSL uses the host via /mnt)
Syncthing.Syncthing
