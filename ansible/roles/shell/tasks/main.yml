---
# Install the shell runtime: zsh + oh-my-zsh + powerlevel10k + Nerd Fonts.
# NOTE: dotfile *content* (.zshrc, .p10k.zsh) is managed by chezmoi, not here.
# This role only installs the programs the dotfiles rely on.

# --- zsh ---
- name: Install zsh (macOS)
  community.general.homebrew:
    name: zsh
    state: present
  when: rl_os == 'macos'

- name: Install zsh (Linux)
  ansible.builtin.package:
    name: zsh
    state: present
  become: true
  when: rl_os == 'linux'

- name: Resolve zsh path
  ansible.builtin.command: command -v zsh
  register: zsh_path
  changed_when: false
  failed_when: false        # tolerate absence (e.g. during --check before install)
  check_mode: false         # read-only: run even in --check

# --- oh-my-zsh ---
- name: Install oh-my-zsh
  ansible.builtin.git:
    repo: https://github.com/ohmyzsh/ohmyzsh.git
    dest: "{{ ansible_facts.env.HOME }}/.oh-my-zsh"
    depth: 1
    version: master
  when: shell_install_omz | bool

# --- powerlevel10k (as an oh-my-zsh custom theme) ---
- name: Install powerlevel10k
  ansible.builtin.git:
    repo: https://github.com/romkatv/powerlevel10k.git
    dest: "{{ ansible_facts.env.HOME }}/.oh-my-zsh/custom/themes/powerlevel10k"
    depth: 1
    version: master
  when: shell_install_p10k | bool

# --- oh-my-zsh custom plugins (referenced by the managed .zshrc) ---
- name: Install zsh custom plugins
  ansible.builtin.git:
    repo: "{{ item.repo }}"
    dest: "{{ ansible_facts.env.HOME }}/.oh-my-zsh/custom/plugins/{{ item.name }}"
    depth: 1
    version: master
  loop:
    - name: zsh-autosuggestions
      repo: https://github.com/zsh-users/zsh-autosuggestions.git
    - name: zsh-syntax-highlighting
      repo: https://github.com/zsh-users/zsh-syntax-highlighting.git
  loop_control:
    label: "{{ item.name }}"
  when: shell_install_plugins | bool

# NOTE: Nerd Fonts are now installed via the dedicated 'fonts' role.

# --- default shell ---
- name: Set default shell
  ansible.builtin.user:
    name: "{{ ansible_facts.user_id }}"
    shell: "{{ zsh_path.stdout }}"
  become: true
  when:
    - shell_set_default | bool
    - shell_default == 'zsh'
    - zsh_path.rc == 0
    - zsh_path.stdout | length > 0
    - zsh_path.stdout not in (ansible_facts.env.SHELL | default(''))
